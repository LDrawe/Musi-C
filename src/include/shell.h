#ifndef SHELL_H
#define SHELL_H
#include "person.h"
void shellsortChoices(int* choices);
void shellsort(person* pessoas, int ARRAY_SIZE);
void shellsortAge(person* pessoas, int ARRAY_SIZE);
void printPersons(person* pessoas);
#endif
